// vestigle hello world.
function main() {
  create_incident.core.hello_world();
}

// Below is a template that can use used for new GoogleAppsScript Sheets functions

/**
 * This text shows the description of the sheet function
 *
 * @param {number} input The type and name of the functions first input
 * @return {number} the type of what comes out
 * @customfunction
 */
function doubleMe(input) {
    return create_incident.core.double_me(num);
}
